# Final Delivery Report — 2026-09-17 (EN)

> Reference copy for the next agent/handover (Astra). Persian original with the
> same content: `docs/RELEASE-2026-09-17.fa.md`. Nothing was deployed, committed,
> or pushed; HEAD is untouched (`dfa2105`); all work lives in the checkout.

## 1) What was done this session

### a) Stage 1 — ElevenLabs crawl diagnosis
**Status: done with limitation** — definitive root cause stays "undetermined"
until the owner supplies the job error text. Everything measurable on the site
side is green (read-only production checks):

- Live `robots.txt`: only `Allow` rules, **zero `Disallow`** → ElevenLabs was
  never blocked (absence of a named section ≠ block).
- Homepage fetched with UA `ElevenlabsBot/1.0`: HTTP 200, `text/html`, 211,640
  bytes, no redirect, `x-litespeed-cache: hit`. Three consecutive requests: all
  200 (no rate limiting).
- Raw HTML (no JS) is content-complete: 46× "Kohandezh", 20× "Kohan System
  Farda", 9× timeline role → server-side extraction is possible; the repo's
  no-JS crawler test (384 assertions) also passes.
- `sitemap.xml`: 200, application/xml. `http://` and `www` resolve cleanly to
  the canonical. `.htaccess` has no anti-bot or UA-blocking rules; CSP does not
  affect server-side crawlers.

**Facts from official ElevenLabs docs (fetched 2026-09-17):**
- Crawler UA is `ElevenlabsBot/1.0`; it respects robots.txt; a blocked fetch
  produces "document created but no content extracted".
- A document must be **attached to the agent** (`knowledge_base` in the agent
  config / "Dependent agents" tab) — the most common reason a doc "isn't used".
- Crawl job params: `max_depth` default 3 (1–5), `max_pages` default 1000,
  optional `pattern` (wrong pattern = zero pages discovered), optional
  `sitemap_urls` (overrides auto-discovery).

**Local code change applied:** explicit `User-agent: ElevenlabsBot / Allow: /`
section added to BOTH `robots.txt` and the `robots_txt` filter in functions.php
(edit-both-together rule). Officially recommended by the docs; a no-op for all
existing bots. CLAUDE.md bot count 15 → 16. The `_tooling/wp-root/` mirror was
refreshed automatically by release prepare. Backup before edit:
`_tooling/backups/kdcv-stage1-elevenlabs-backup.tar.gz`.

**4 data points needed from the owner for a definitive verdict:** error
text/screenshot, URL entered, time of the attempt, import type (Single URL /
Website Crawl / Sitemap). Owner-side (optional): grep LiteSpeed access logs for
`ElevenlabsBot` and 403/429/5xx/timeouts. Open hypothesis (unverifiable from
outside): network reachability from ElevenLabs' crawler IPs to the Iran-hosted
server.

### b) Default language = Persian (owner request)
**Already implemented; re-verified, no change needed:**
- `assets/js/locale-router.js:34` → `DEFAULT_LOCALE = "fa"` (no-signal visitor → Persian).
- `functions.php:632` → WP geo-router redirects no-signal humans to `/fa/`, AFTER the bot check.
- Genuinely English browsers stay on English; bots index `/` as the English
  canonical (CLAUDE.md convention #5).
- If the owner wants even English-browser humans forced to fa on first visit:
  small change, but it alters English traffic and canonical behaviour — only
  with explicit OK (not recommended).

### c) New plugin: KDCV Right-Click Guard v1.0.0
- Source: `_tooling/wp-theme/kdcv-rightclick-guard/kdcv-rightclick-guard.php` (single file).
- ON/OFF switch at `wp-admin → Settings → Right-Click Guard`; default **ON**
  (context menu blocked for visitors). wp-admin is never affected.
- Tiny inline vanilla JS (`preventDefault` on `contextmenu`) printed in the footer.
- Security: `manage_options` capability, nonce via `settings_fields()`, boolean
  sanitizer, uninstall hook deletes the option.
- Side change: `_tooling/wp-local/docker-compose.yml` gained a read-only mount
  for the plugin (wordpress + cli services).
- **Real local WP test (localhost:8888):** wp-cli activation ✓; ON → guard
  present on `/` and `/fa/` ✓; option=0 → zero occurrences ✓; back to 1 →
  present again ✓; settings page renders as admin (RENDER_OK, 1519 bytes) ✓;
  debug.log has no new errors ✓.
- Limitation: the agent did not perform a real-browser right-click; the logic
  is the standard preventDefault pattern, DOM-verified.
- Production note: after toggling, **purge LiteSpeed cache** or cached pages
  keep the old behaviour.

## 2) Final test battery (all green)

| Test | Result |
|---|---|
| `npm test` | exit 0; 695 PASS incl. 384 crawler, 293 llms, 12 wp-links, 16 jquery-shim |
| `bash _tooling/tests/run.sh` (dev server :8735) | PASS=32 FAIL=0 |
| `sync-from-static.py --dry-run` | parity; 0 create / 0 update / 0 delete (17 templates, 497 assets) |
| `git diff --check` | clean |
| `php -l` functions.php + new plugin | no errors |
| `release.py prepare` | 16 root files + theme ZIP integrity; no production writes |
| ZIP scans | zero .DS_Store / __MACOSX / .bak / backups / secret-looking strings |

## 3) Packages and SHA-256

| Package | Path | SHA-256 |
|---|---|---|
| WordPress theme (22.7 MB) | `_tooling/wp-theme/kohandezhcv.zip` | `229c1c53ea785fc8ac07c56e27e902744e4ca357eaa0264123ee0021f97b7a5a` |
| Root files (16 files) | `_tooling/releases/current/root-files.zip` | `fd88bd5ee4c47dad84247a6443f40d364269cd388291e309a3a5fba26b640d6f` |
| Pet plugin | `_tooling/releases/current/kohan-avatar.zip` | `987f959af27ebcf88a96e3c5730c4a8b99706f95e7e18759d265460faa317032` |
| Right-Click Guard plugin | `_tooling/releases/current/kdcv-rightclick-guard.zip` | `a48727a824fac507e34d0d61bc2280e27823eb3b44a4589fdb5b6e44a663d681` |

- Manifest: `_tooling/releases/current/release-manifest.json` (theme hash + root
  files + 6 key theme assets).
- root-files.zip contents: `robots.txt` (ElevenlabsBot + `Sitemap:
  https://kohandezh.com/?kdcv_sitemap=index`), `sitemap.xml`, `feed.xml`,
  `sw.js`, `manifest.json`, `offline.html`, 10× `llms.txt`.
- The old delta ZIPs in `_tooling/wp-theme/` (kohan-*.zip, old kohan-avatar.zip,
  kohandezh-ai-hub.zip) are **NOT** this release's output. kohandezh-ai-hub was
  not modified this session and has no new package.

## 4) Host application guide (owner, in order)

**Phase 0 — backup (mandatory):** from the host file manager/FTP download a ZIP
of `wp-content/themes/kohandezhcv/`, the `kohan-avatar` and `kohandezh-ai-hub`
plugin folders, and the current 16 root files next to `wp-config.php` (these are
the rollback copies). Optional: wp-admin → Tools → Export.

**Phase 1 — theme:** wp-admin → Appearance → Themes → Add New → Upload Theme →
`kohandezhcv.zip` → Replace active with uploaded.

**Phase 2 — plugins:** Plugins → Add New → Upload → `kohan-avatar.zip` →
Replace active. Then `kdcv-rightclick-guard.zip` → Install → **Activate**. Then
Settings → Right-Click Guard → pick ON/OFF (default ON = right-click blocked).

**Phase 3 — root files:** extract root-files.zip and upload/overwrite all 16
files in the web root (next to wp-config.php).

**Phase 4 — purge:** wp-admin → LiteSpeed Cache → Purge All (+ any CDN cache).

**Phase 5 — migrations:** visit any wp-admin page once
(`kdcv_content_schema_version` auto-migrates). If 404s appear: Settings →
Permalinks → Save once.

**Rollback:** re-upload the Phase-0 backups through the same flows (theme,
plugins), restore the 16 root files, purge again.

## 5) Post-deploy checklist

- [ ] `curl -sI https://kohandezh.com/` → 200; exactly one `rel="canonical"`
- [ ] Live robots.txt contains `ElevenlabsBot` and the `kdcv_sitemap=index` line
- [ ] `/fa/` Persian; no-signal visitor → Persian; bots stay on English `/`
- [ ] A missing URL → 404 arcade with HTTP 404 status
- [ ] Both Elecomp articles answer 200
- [ ] Pet: one instance, one spritesheet URL; chat opens; "Let's talk" → #contact
- [ ] Right-click blocked on the frontend, free in wp-admin; toggle works after a purge
- [ ] Search Console: request re-indexing of `/` and `/fa/`
- [ ] ElevenLabs: retry the crawl, attach the document to the agent; if it fails
      again send the 4 data points from §1a
- [ ] Measure real speed on the host (PageSpeed) — local numbers do not
      represent the host

## 6) Honest limitations

- No deploy performed by the agent; hashes verified locally only.
- No real-browser right-click event test.
- No test message sent to any AI provider; Live Avatar never enabled (its
  containers stayed stopped).
- Ten-language visual QA, cold-cache benchmarks and host-speed measurements not
  done (stages 2–6 pending owner OK).
- ElevenLabs definitive cause: "undetermined with available evidence" without
  the owner's job error text / server logs.

## 7) Remaining work (awaiting owner OK)

Stage 2 fonts/accessibility → 3 runtime performance → 4 SEO/GEO final → 5
feature/chatbot status → 6 ten-language QA. Nothing committed; all changes
preserved in the working tree. Session report also recorded in `WORK-LOG.md`
(2026-09-17 entry) and `HANDOVER-STATUS.md` (top note).
