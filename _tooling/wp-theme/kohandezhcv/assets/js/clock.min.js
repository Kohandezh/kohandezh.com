/*!
 * clock.js — self-contained local date/time for every page.
 *
 * No external service, no network, no timezone database: `Intl.DateTimeFormat`
 * is built into the browser and already knows the visitor's own timezone and
 * calendar. A fetch to a time API would be slower, would leak the visit, and
 * would break offline — the device clock is the right source here.
 *
 * Formats in the PAGE's language (Persian pages get the Solar Hijri calendar
 * via fa-IR, Japanese gets 2026年8月2日, etc). main.js used to render this with
 * a hardcoded en-GB / en-US format, so every non-English page showed an English
 * date; that implementation stands down when this file is present.
 *
 * Markup contract (already present on the CV pages):
 *   <div class="time-local"><p class="date"></p><p class="clock"></p></div>
 */
!function(){"use strict";window.__KDCV_CLOCK_ACTIVE__=!0;var e={en:"en-US",fa:"fa-IR",ar:"ar-EG",de:"de-DE",es:"es-ES",fr:"fr-FR",tr:"tr-TR",ja:"ja-JP",zh:"zh-CN"};function t(){var t=document.querySelectorAll(".time-local");if(t.length){var o,r,a,i=(o=(document.documentElement.getAttribute("lang")||"en").toLowerCase(),e[o.split("-")[0]]||"en-US");try{r=new Intl.DateTimeFormat(i,{weekday:"short",month:"short",day:"numeric"}),a=new Intl.DateTimeFormat(i,{hour:"2-digit",minute:"2-digit",hour12:!1})}catch(e){return}d(),n.boundary&&window.clearTimeout(n.boundary),n.minute&&window.clearInterval(n.minute),n.boundary=window.setTimeout(function(){d(),n.minute=window.setInterval(d,6e4)},1e3*(60-(new Date).getSeconds()))}function d(){for(var e=new Date,n=r.format(e),o=a.format(e),i=0;i<t.length;i++){var d=t[i].querySelector(".date"),u=t[i].querySelector(".clock");d&&d.textContent!==n&&(d.textContent=n),u&&u.textContent!==o&&(u.textContent=o)}}}var n={};"loading"===document.readyState?document.addEventListener("DOMContentLoaded",t,{once:!0}):t(),window.addEventListener("kdcv:page-i18n",function(){t()})}();