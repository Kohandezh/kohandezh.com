> **به‌روزرسانی ۱۴۰۵/۰۶/۲۶ (نشست بعد از GLM):**
> - **باگ راست‌کلیک رفع شد.** علت: هر ۱۴ صفحهٔ HTML یک هندلر `contextmenu` درون‌خطی
>   داشتند که کلید افزونه را نمی‌خواند، پس خاموش‌کردن Guard فقط لیسنر خود افزونه را
>   برمی‌داشت. حالا هندلر درون‌خطی `window.KDCV_RIGHTCLICK_GUARD` را می‌خواند و افزونه
>   (v1.1.0) این پرچم را در `wp_head` با اولویت ۱ و **در هر دو حالت** چاپ می‌کند.
>   تست واقعی مرورگر روی وردپرس محلی: ON → مسدود + toast، OFF → راست‌کلیک آزاد.
> - **علت crawl نشدن ElevenLabs پیدا شد:** `robots.txt` روی production برای هر
>   user-agent **403** برمی‌گرداند — همراه ۱۴ فایل root دیگر (هر ده `llms.txt`،
>   `sw.js`، `manifest.json`، `feed.xml`، `offline.html`). جزئیات و راه رفع در
>   `docs/CRAWL-403-2026-09-17.fa.md`. رفع آن **فقط از سمت هاست** ممکن است.
> - **نام فارسی به دادهٔ ساختاریافته اضافه شد:** موجودیت Person هیچ
>   `alternateName` نداشت و صفحهٔ فارسی هیچ نام فارسی در schema نداشت. حالا ۲۸
>   املای همان نام در `assets/data/identity.json` و از آنجا در ۱۰ فایل HTML و ۱۰
>   فایل llms قرار دارد.

# وضعیت تحویل و پیشرفت پروژه (HANDOVER-STATUS)

## به‌روزرسانی: ۲۰۲۶-۰۹-۱۷ (آخر)

بستهٔ نهایی ساخته و صحت‌سنجی شد؛ گزارش کامل در `docs/RELEASE-2026-09-17.fa.md`. این نشست: اجازهٔ صریح `ElevenlabsBot` در robots.txt + functions.php (تشخیص ElevenLabs: سمت سایت سبز، علت قطعی منتظر ۴ دادهٔ مالک)، افزونهٔ جدید Right-Click Guard با کلید ON/OFF (تست‌شده روی وردپرس محلی)، بازآزمایی زبان پیش‌فرض فارسی (از قبل فعال)، بازسازی ۴ بسته با هش، و تست نهایی کامل (npm 695 PASS، shell 32/0، sync parity، git diff --check تمیز). هیچ انتشار یا commit انجام نشده. جدول ۹مرحله‌ای پایین سابقهٔ قدیمی است؛ شماره‌گذاری فعلی: ۱ ElevenLabs، ۲ فونت/دسترس‌پذیری، ۳ بهینه‌سازی اجرا، ۴ SEO/GEO، ۵ قابلیت‌ها/چت‌بات، ۶ QA ده زبان، ۷ بسته‌بندی (انجام شد).

## به‌روزرسانی: مراحل محلی ۱ و ۲

مرحلهٔ ۳ تصاویر نیز تکمیل شد؛ گزارش `docs/LOCAL-IMAGES-NETWORK-QA.md` شامل اندازه‌ها و تست واقعی مرورگر/Network روی WordPress محلی است. تست متمرکز روی انگلیسی/فارسی/عربی انجام شد، نه تأیید کامل ظاهری ده زبان. هیچ انتشار یا commit انجام نشده است.

دستور فعلی مالک بر برنامهٔ قدیمی پایین این فایل مقدم است: همهٔ کارها فقط محلی؛ انتشار با مالک و پیشروی مرحله‌ای با OK. شماره‌های فعلی: ۱ بررسی نسخهٔ تحویلی، ۲ رفع دانلود تکراری Pet. هر دو انجام شدند؛ گزارش تازه در `docs/LOCAL-PET-DEDUP.md` است. بخش‌های انتشار و هش‌های قدیمی پایین، سابقه‌اند نه وضعیت بستهٔ فعلی. بستهٔ ریشه ۱۶ فایل دارد، نه ۱۵ فایل.

**پروژه:** تکمیل اصلاحات و بهینه‌سازی kohandezh.com  
**تاریخ آخرین بررسی:** ۲۷ شهریور ۱۴۰۵ / 2026-09-17  
**نقش:** مهندس ارشد WordPress، Frontend، Performance و Technical SEO  

---

## ۱. اصول حاکم و منبع حقیقت

- **منبع حقیقت محتوا و ساختار:** سایت استاتیک در ریشهٔ مخزن (`index.html` و ۹ زبان دیگر).
- **قالب وردپرس:** در مسیر `_tooling/wp-theme/kohandezhcv/` قرار دارد و به‌وسیلهٔ `sync-from-static.py` از روی سایت استاتیک بازتولید می‌شود. ویرایش دستی تمپلیت‌های PHP تولیدشده ممنوع است (`functions.php` و فایل‌های مشخص دست‌ساز استثنا هستند).
- **فایل‌های ریشه:** ۱۵ فایل شامل ۱۰ نسخهٔ `llms.txt`، `robots.txt`، `sitemap.xml`، `feed.xml`، `sw.js`، `manifest.json` و `offline.html` در بستهٔ `_tooling/releases/current/root-files.zip` قرار دارند و باید در ریشهٔ هاست (کنار `wp-config.php`) قرار گیرند.
- **تغییرات commitنشده:** تغییرات کاری جاری در checkout حفظ شده‌اند و هیچ دستور مخرب (`git reset --hard` یا `git clean`) اجرا نمی‌شود.
- **حفظ اصول هویتی و داده‌ها:**
  - تاریخ‌های تأییدشدهٔ شغلی (KSF: Dec 2013 – Present / مدبران: Jul 2018 – Feb 2024).
  - حفظ دکتری اصلی با نگارش «دکتری» و عدم بازگشت ادعای دکتری دوم/PhD Candidate.
  - حفظ نقش و توضیح سکو (Developer — National Open-Source AI Platform) و لوگوی اصلی بدون تصویرسازی اضافه.
  - حفظ پیام‌رسان‌ها (Telegram @kohandezh, Bale @kohandezh, Eitaa @kohandezhh, WhatsApp +18106662283) با آیکن‌های اختصاصی.
  - مقصد گفتگو به `#contact` و حفظ تعامل و اسکرول بدون پرش ناخواسته.
  - حفظ Pet فعال و عدم فعال‌سازی Live Avatar لانچ‌نشده.

---

## ۲. خلاصهٔ وضعیت مراحل ۹‌گانه

| مرحله | عنوان | وضعیت | توضیحات |
| :---: | :--- | :---: | :--- |
| **۱** | **ادغام محلی (Local Integration)** | **تکمیل و تأیید ۱۰۰٪** | همهٔ تست‌ها سبز، برابری کامل sync، پشتیبان پایدار ایجاد شد. |
| **۲** | **انتشار امن نسخهٔ یکپارچه (Safe Deployment)** | **آماده برای تأیید مالک** | بسته‌ها و هش‌ها آماده؛ نیازمند تأیید صریح مالک و تعیین روش اتصال/آپلود. |
| **۳** | **حذف بارگذاری تکراری Pet/Avatar** | در انتظار شروع | پس از اتمام مرحلهٔ ۲ |
| **۴** | **بهینه‌سازی تصاویر (Responsive/WebP/LCP)** | در انتظار شروع | پس از اتمام مرحلهٔ ۳ |
| **۵** | **فونت و دسترس‌پذیری (a11y/icomoon/RTL)** | در انتظار شروع | پس از اتمام مرحلهٔ ۴ |
| **۶** | **بهینه‌سازی اجرای صفحه (Runtime/Reflow/Cache)** | در انتظار شروع | پس از اتمام مرحلهٔ ۵ |
| **۷** | **SEO/GEO و انتشار محتوا (hreflang/sitemap/feed)** | در انتظار شروع | پس از اتمام مرحلهٔ ۶ |
| **۸** | **QA ظاهری و عملکردی (۱۰ زبان/Mobile/Desktop)** | در انتظار شروع | پس از اتمام مرحلهٔ ۷ |
| **۹** | **سنجش نهایی و تحویل (Lighthouse/Tests/Handover)** | در انتظار شروع | مرحلهٔ پایانی |

---

## ۳. گزارش دقیق مرحلهٔ ۱ (ادغام محلی — انجام و تأیید مجدد شد)

### موارد بررسی‌شده و شواهد:
1. **تست‌های کامل پروژه (`npm test`):**
   - خروجی: خروج با کد صفر (موفق).
   - شامل: `wisdom-quotes`, `jquery-shim`, `cv/build`, `credentials-regression` (حذف دکتری دوم، حفظ دکتری اصلی، تطابق تاریخ‌های KSF و مدبران در ۱۰ زبان و PDFهای ATS), `llms`, `crawler` (۳۸۴ assertion), `wp-links`, `frontend-regressions`, `publication` (متادیتای مقاله و سایت‌مپ بدون دستکاری تاریخ/نویسنده), `release-package`.
2. **مجموعه تست‌های Shell (`bash _tooling/tests/run.sh`):**
   - ۳۲ تست موفق، صفر شکست (`PASS=32, FAIL=0`).
   - سلامت لایهٔ A و تفکیک لایهٔ B تأیید شد.
3. **تطابق همگام‌سازی استاتیک به وردپرس (`sync-from-static.py --dry-run`):**
   - ۱۷ قالب و ۴۹۷ فایل asset کاملاً منطبق (`0 create | 0 update | 0 delete`).
4. **بررسی گیت (`git diff --check`):**
   - بدون خطای فاصله‌گذاری یا تضاد.
5. **اعتبارسنجی بسته‌های آماده‌سازی انتشار:**
   - قالب: `_tooling/wp-theme/kohandezhcv.zip`  
     هش SHA-256: `6f28aa1c580fe2878364ec15fccd46226c6f273f2c8eee1889889e2e73f06227`
   - ریشه: `_tooling/releases/current/root-files.zip`  
     هش SHA-256: `12735c3538e3ae0ab5edc2d197ef54cc73f68ba623c74d3e966898978b2fe706`
   - مانیفست انتشار: `_tooling/releases/current/release-manifest.json`
6. **پشتیبان پایدار و ایمن (حل وابستگی به پوشهٔ موقت سیستم):**
   - پشتیبان موقت سیستم قبلی به پوشهٔ دائمی و امن منتقل و آرشیو شد:
     - دایرکتوری: `_tooling/backups/kdcv-stage1-backup-gb9f1ef0`
     - آرشیو فشرده: `_tooling/backups/kdcv-stage1-backup.tar.gz` (حجم ۶.۳ مگابایت)
     - مسیر در `.gitignore` ثبت شد تا مانع آلودگی مخزن شود.
7. **وضعیت زندهٔ Production (`release:verify` و curl):**
   - سرور فعال: LiteSpeed در `https://kohandezh.com/` (پاسخ HTTP 200).
   - دو مقالهٔ الکامپ روی پروداکشن پاسخ ۲۰۰ دارند:
     - `/2025/09/29/ayno-tv-elecomp-1404/`
     - `/2025/10/01/elecomp-talks-1404-ai-for-children/`
   - راستی‌آزمایی عمومی با مانیفست انتشار نشان داد که **۲۰ فایل در پروداکشن با بستهٔ محلی تفاوت دارند** (پروداکشن هنوز نسخهٔ قدیمی را اجرا می‌کند و بستهٔ جدید روی آن نصب نشده است). این موضوع صحت ادعای عدم انتشار را تأیید می‌کند.

---

## ۴. آمادگی برای مرحلهٔ ۲ (انتشار امن نسخهٔ یکپارچه)

### پیش‌نیازهای دروازهٔ ورود به مرحلهٔ ۲:
- [x] بسته‌ها و فایل‌های ریشه ساخته شده و هش آن‌ها ثبت است.
- [x] تست‌های یکپارچگی محلی همگی پاس هستند.
- [x] پشتیبان پایدار محلی از فایل‌های منبع قبل از انتشار در دسترس است.
- [ ] دریافت تأیید صریح مالک برای آغاز انتشار روی Production.
- [ ] تعیین کانال دسترسی به محیط WordPress سرور (wp-admin / FTP / File Manager / SSH).

### طرح بازگردانی (Rollback Plan) برای مرحلهٔ ۲:
1. پیش از اعمال روی Production: تهیهٔ بکاپ کامل از پوشهٔ `wp-content/themes/kohandezhcv/` فعال و فایل‌های ریشهٔ زنده.
2. در صورت بروز هرگونه اختلال:
   - بازگردانی پوشهٔ قالب از بکاپ قبلی.
   - بازگردانی فایل‌های ریشه از بکاپ ریشه.
   - بازتولید کش WP-Optimize و پاک‌سازی کش LiteSpeed.
   - ارزیابی پاسخ ۲۰۰ مسیرهای اصلی و بازگشت به شرایط پایدار قبلی.
